version: 2

models:
  - name: stg_events
    description: This table contains information on Greenery's event data
    columns:
      - name: event_id
        tests:
        - unique
        - not_null
